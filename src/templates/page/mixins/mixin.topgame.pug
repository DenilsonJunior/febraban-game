mixin topgame(nome)
    .boxTop
        h2.title= nome
        .info
            video(src="../../assets/video/timer.webm" autoplay muted loop) 
            span.value {{ time }} 
            //- div
            //- 	span.label Turns:
            //- 	span.value {{ turns }} 